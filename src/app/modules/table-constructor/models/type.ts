export class TypeField {
    constructor
    (
        simple_type: string,
        directory_id: string,
        instance_directory_id: string,
        task_id: string | null
    ) {
        this.simple_type = simple_type,
        this.directory_id = directory_id,
        this.instance_directory_id = instance_directory_id,
        this.task_id = task_id
      }
      simple_type: string;
      directory_id: string;
      instance_directory_id: string;
      task_id: string | null;
}

export class TypeFieldto {
    constructor
    (
        simple_type: string,
        directory_id: string,
        instance_directory_id: string,
        task_id: string | null
    ) {
        this.simple_type = simple_type,
        this.directory_id = directory_id,
        this.instance_directory_id = instance_directory_id,
        this.task_id = task_id
      }
      simple_type: string;
      directory_id: string;
      instance_directory_id: string;
      task_id: string | null;
}